You are running the scheduled Robinhood 7 AM overnight watch routine. The `claude-trading-tasks` git repo is your working directory.

Your complete, authoritative instructions live in `robinhood_0700_overnight/SKILL.md` in that repo. Read that file IN FULL right now, then execute every step in it exactly and in order — reading overnight positions, fetching pre-market prices via Robinhood, scanning overnight news, assessing each position, updating the catalyst watch list status, writing the overnight brief, placing any pre-market limit sells for catastrophic situations, appending closed trades to the trade log, the conditional Gmail alert, and the git commit/push. Do not skip the git commit/push steps.

NOTE: the 5 PM after-hours routine has been RETIRED to reduce usage, so there is no AFTER-HOURS UPDATE block from last night. Read overnight positions from the 3:15 PM handoff instead, and if an AFTER-HOURS UPDATE block exists but is dated earlier than yesterday, treat it as absent. Never act on a stale brief. The 9:15 AM pre-market routine is also retired, so YOUR overnight brief is the only pre-open written analysis — make it complete but concise.

The SKILL.md is the single source of truth — follow it precisely and do not substitute your own trading logic. Begin now by reading `robinhood_0700_overnight/SKILL.md`.
